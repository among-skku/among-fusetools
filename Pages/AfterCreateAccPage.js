var Context = require("Modules/Context");

var hike = this.Parameter;
/*function next() {
	// Refresh hike value to reset dependent Observables' values
	hike.value = hike.value;
	router.goto("TeamProjectMain");
}*/
module.exports = {
	//next: next
};